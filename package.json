{
  "name": "newsforge",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "sites/*",
    "pipeline"
  ],
  "scripts": {
    "dev": "npm run dev --workspace sites/tabletnexus",
    "build": "npm run build --workspace sites/tabletnexus",
    "generate": "npm run generate --workspace pipeline --"
  }
}

[defaults]
inventory = inventory/local.yml
roles_path = roles
host_key_checking = False
interpreter_python = auto_silent
# Nota: eseguito da WSL con il repo su /mnt/d, lanciare con
#   export ANSIBLE_CONFIG="$PWD/ansible.cfg"
# perché Ansible ignora un ansible.cfg in una cartella "world-writable".
